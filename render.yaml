services:
  - type: web
    name: cipher-lens
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: CIPHER_LENS_SECRET
        generateValue: true
